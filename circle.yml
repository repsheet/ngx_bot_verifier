dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install build-essential zlib1g-dev libpcre3-dev
    - PERL_MM_USE_DEFAULT=1 cpan Test::Nginx
    - script/bootstrap
test:
  override:
    - make test
